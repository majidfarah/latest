# STEM Guide AI Service

FastAPI microservice that generates learning path recommendations based on a subject, level, and optional goals list.

## Running locally

```bash
pip install -r requirements.txt
uvicorn app:app --reload --port 8000
```
